"""Identity-centric behavioural analysis: typing identities by how they behave.

The home for non-human-identity (NHI) modelling. Its first capability is typing
an identity as automated vs human from the temporal shape of its activity alone
(``typing.py``), which is the prerequisite for any NHI-specific baseline: a
service account and a person are statistically different populations and must not
share one model.
"""
