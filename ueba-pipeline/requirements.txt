# Runtime dependencies.
#
# Version floors are set to the API each module actually uses; upper bounds stop
# a major release from silently changing numerical behaviour in a detector.

numpy>=2.0,<3            # counters, quantile grids, scoring
scipy>=1.11,<2           # scipy.stats: chi2/norm for the p-value combiners
pydantic>=2.5,<3         # config schema validation
PyYAML>=6.0,<7           # config files
networkx>=3.0,<4         # identity graph (analyst tooling); NOT on the detection path

# -- evaluation only. Imported by evaluation/model_benchmark.py, which compares
#    classical baselines against the shipped detector. Not on the detection path,
#    but required for `model-benchmark` to run.
scikit-learn>=1.3,<2
xgboost>=2.0,<4          # one entry in the model comparison; imported lazily

# -- optional, feature-gated. Guarded at the import site; the core engine runs
#    without it and falls back to file ingestion.
confluent-kafka>=2.5,<3  # ingestion/source.py KafkaEventSource
